<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/undo"
        android:enabled="false"
        android:icon="@drawable/baseline_undo_24"
        android:title="@string/undo"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/redo"
        android:enabled="false"
        android:icon="@drawable/baseline_redo_24"
        android:title="@string/redo"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/save"
        android:enabled="false"
        android:title="@string/save" />

    <item android:title="@string/search">
        <menu>
            <item
                android:id="@+id/search_inside_file"
                android:enabled="false"
                android:title="@string/search_inside_file" />
        </menu>
    </item>

    <item
        android:id="@+id/close_project"
        android:title="@string/close_project" />

</menu>